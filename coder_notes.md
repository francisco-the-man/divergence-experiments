Revisions vs prior submission: (1) detect_n_vars now uses sympy free_symbols — fixes the digit-scanning bug that inflated n_vars for T3 and T5 and contaminated NN measurement. (2) Primary late-phase segmentation now uses start_fit <= median(final_fit) per the plan; gen-midpoint version retained as sigma_lognormal_late_genmid for Avery to cross-check. (3) per-rep sigma loop now iterates over rep indices actually present in all_durations. Caveats unchanged from before: per-rep wall budget ~12s with 120 reps × 5 targets × 2 conditions on a 1vCPU/30min container is tight — reps may be wall-capped before hitting max_generations=4000; if Avery finds completion rates <60% across tasks, suggest dropping n_replicates to 60 and raising per-rep budget. NN estimate is a behavioral-fingerprint proxy for true e-graph semantic-class size (faithful e-graph saturation would need egglog/snake-egg, not currently installed); flagging this as the most methodologically optimistic piece.